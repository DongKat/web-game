// Draw everything
import { Tile } from "components/Tile.ts";
import { Unit } from "components/Unit.ts";
import { Building } from "components/Building.ts";
import { Board } from "map/Board.ts";
import type { Terrain } from "components/Terrain.ts";


export class Renderer {
    constructor() {
    }

    renderBoard(board: Board) {
    }

    renderUnit(unit: Unit) {
    }

    renderTerrain(terrain: Terrain) {
    }

    renderBuilding(building: Building) {
    }
}